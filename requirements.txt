# Exact versions the test suite last passed against. Pinned because the
# code relies on things a loose ">=" range doesn't promise: the anthropic
# SDK's output_config (structured outputs and effort), moviepy 1.x's
# editor API, and Pillow 9 (moviepy 1.0.3 breaks on Pillow 10's removal
# of Image.ANTIALIAS). Upgrade deliberately: bump one line, run
# `python -m pytest`, commit both together.
#
# Everything the code imports directly is listed, including numpy, proglog
# and imageio-ffmpeg, which used to arrive only as moviepy's dependencies.
# Transitive dependencies are not pinned here.

# Runtime
anthropic==1.1.0
faster-whisper==1.2.1
flask==3.1.3
werkzeug==3.1.8
imageio-ffmpeg==0.6.0
moviepy==1.0.3
numpy==1.25.2
pillow==9.5.0
playwright==1.63.0
proglog==0.1.12
requests==2.31.0

# Development
pytest==9.1.1
